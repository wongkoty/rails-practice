<% provide(:title, @user.name) %>
<title>Ruby on Rails Sample App | <%= yield(:title) %></title>

<div class="row">
	<aside class="span4">
		<section>
			<h1> 
				<%= gravatar_for @user %>
				<%= @user.name %>
			</h1>
		</section>
	</aside>
</div>